// import express from 'express';
// import restrictLocalAccess from '../middleware/restrictLocal';
// import {
//   getExamSubjects,
//   startExam,
//   submitExam,
//   examLogin,
// } from '../controllers/exam.controller';

// const router = express.Router();

// router.use((req, res, next) => restrictLocalAccess(req, res, next));

// router.post('/login', examLogin);
// router.post('/start', startExam);
// router.post('/submit', submitExam);
// router.get('/subjects', getExamSubjects);

// export default router;
